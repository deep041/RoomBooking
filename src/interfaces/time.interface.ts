export interface Time {
    from?: string,
    to?: string,
    isOwnerSelected?: boolean,
    isSelected?: boolean,
    isSelectionStarted?: boolean
}

export interface SelectedTime {
    from?: string,
    to?: string
}